{
  "name": "esp-tui",
  "onCreateCommand": "sudo apt-get update && sudo apt-get install -y libudev-dev binutils",
  "postCreateCommand": "cargo install espup && espup install && rustup target add riscv32imc-unknown-none-elf riscv32imac-unknown-none-elf"
}
