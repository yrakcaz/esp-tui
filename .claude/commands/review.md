---
description: "Review Rust code changes in esp-tui using the reviewer agent"
allowed-tools: ["Agent"]
---

Spawn the `reviewer` agent to review the current branch's changes.

Use the Agent tool with `subagent_type: "reviewer"`. Pass any arguments provided as context: "$ARGUMENTS"
