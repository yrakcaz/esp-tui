[workspace]

[package]
name = "esp-agent-example"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
esp-idf-sys = { version = "0.37", features = ["binstart"] }

[build-dependencies]
embuild = { version = "0.33", features = ["espidf"] }
